# SSL 인증서 경로 (회사 프록시 환경에서만 필요)
# 설정하면 ios-watcher.js와 server.js에서 NODE_EXTRA_CA_CERTS로 자동 적용됩니다.
# CERT_PATH=/path/to/your/proxy.crt

# Anthropic API 키 (Settings > 컴포넌트 매핑의 화면 자동 요약 기능에 필요)
# ANTHROPIC_API_KEY=sk-ant-...
